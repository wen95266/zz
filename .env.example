
# ==========================================
# Termux Alist Bot 配置文件模板
# 使用方法: 复制此文件内容到 ~/.env 并填入你的信息
# ==========================================

# ------------------------------
# 必填项 (必须配置)
# ------------------------------

# 1. Telegram Bot Token
# 向 @BotFather 发送 /newbot 申请
BOT_TOKEN=

# 2. 管理员 ID
# 向 @userinfobot 发送消息获取 ID，只有此 ID 能控制机器人
ADMIN_ID=

# 3. 隧道模式
# quick: 使用 Cloudflare Quick Tunnel (每次启动随机域名，无需 Token)
# token: 使用 Cloudflare Zero Trust (固定域名，推荐)
TUNNEL_MODE=quick

# ------------------------------
# 选填项 (根据需求配置)
# ------------------------------

# 4. Alist 管理员密码 (强烈推荐)
# 填写此项后，Bot 将直接使用此密码登录 Alist，不再尝试自动抓取，极其稳定。
# 请确保此密码与 ./set_pass.sh 设置的一致。
ALIST_PASSWORD=

# 5. Alist Token (高级可选)
# 如果你不想用密码，可以在 Alist 管理面板生成一个长期 Token 填入此处。
# 如果填了 Token，ALIST_PASSWORD 将被忽略。
ALIST_TOKEN=

# 6. Cloudflare Tunnel Token
# 仅当 TUNNEL_MODE=token 时必填。在 Zero Trust 面板获取。
CLOUDFLARE_TOKEN=

# 7. Alist 自定义域名
# 仅当 TUNNEL_MODE=token 时建议填写 (不带 http://)
# 如果是 quick 模式，留空即可，Bot 会自动抓取随机域名
ALIST_DOMAIN=

# 8. 直播推流地址
# 用于 /stream 命令。在 TG 频道开启直播后获取 rtmp 地址
TG_RTMP_URL=

# 9. Aria2 RPC 密钥
# 建议设置以增强安全性。留空则不启用密码验证。
ARIA2_RPC_SECRET=

# 10. GitHub 账号池 (仅用于 /stream 推流功能)
# 格式: 用户名/仓库名|Token
GITHUB_ACCOUNTS_LIST=
